for (int i = 0, j = 0; i < board.length && j < board.length; i++, j++) {

        //     if (((i + j) == (row + col)) || ((i - j) == (row - col)) || (i == row) || (j == col)) {
        //         return false;

        //     }
        // }